name: mobile_banking_app
description: Мобильное приложение для безопасного и удобного банкинга.

# Версия приложения. Первая цифра - мажорная (большие изменения), вторая - минорная (новые функции), 
# третья - патч (исправления). Номер сборки (1) увеличивается при каждой сборке.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Требуемая версия Dart SDK

# ------------------------------------
# 📦 ЗАВИСИМОСТИ (Dependencies)
# ------------------------------------
dependencies:
  flutter:
    sdk: flutter
  
  # UI и навигация
  cupertino_icons: ^1.0.6    # Иконки Apple-style
  go_router: ^14.0.0         # Мощный пакет для навигации (роутинг)
  
  # Управление состоянием (пример использования BLoC/Cubit)
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5          # Помогает сравнивать объекты BLoC

  # Работа с данными и сетью
  dio: ^5.4.3                # HTTP-клиент для API (более мощный, чем стандартный http)
  json_annotation: ^4.8.1    # Для генерации кода моделей JSON (Serialization/Deserialization)
  
  # Локальное хранение (безопасное хранение токенов и чувствительных данных)
  flutter_secure_storage: ^9.0.0 # Шифрованное хранение данных (ключи, токены)
  
  # Локализация
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0              # Интернационализация и форматирование даты/валют

  # Дополнительные функции
  url_launcher: ^6.2.6       # Открытие ссылок, звонков, почты
  
# ------------------------------------
# 🛠️ DEV-ЗАВИСИМОСТИ (Dev Dependencies)
# ------------------------------------
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0      # Набор правил для анализа кода (статический анализ)
  
  # Генерация кода (для json_annotation)
  build_runner: ^2.4.9
  json_serializable: ^6.7.1  

# ------------------------------------
# 🖼️ Р
